# Static assets

Place your brand logo here as **`Polad.png`** (ideally a PNG with a
transparent background for the cleanest look inside the glowing badge).

```
frontend/public/Polad.png
```

It is automatically used by `src/components/Logo.jsx`. If the file is
missing, the component falls back to the built-in SVG logo, so the app
never breaks.
